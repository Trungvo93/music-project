import SongRecently from "@/features/leftSide/menuPlaylist/dropPlaylist/recently/SongRecently";

const page = () => {
  return (
    <div>
      <SongRecently />
    </div>
  );
};

export default page;
